(ns core
    (:require
      [clojure.string :as str]
      [medley.core :as md]))                                ;; <- fails here

(defn -main [& args]


      (println '-main 'running args)


      )